diff --git a/dist/index.mjs b/dist/index.mjs
index 8272f163ff3554fed4763aa2141b1f12ddf46615..e6cabe1df23789f822fea7984aa84f764c979e72 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -21,7 +21,7 @@ function loadNative() {
 		return null;
 	}
 }
-const native$1 = loadNative();
+const native$1 = process.env.ZIGPTY_DISABLE_NATIVE === "1" ? null : loadNative();
 const hasNative = native$1 !== null;
 var WriteQueue = class {
 	_queue = [];
@@ -715,7 +715,12 @@ var PipePty = class extends BasePty {
 				}
 			}
 			const sig = SIGNAL_CHARS[byte];
-			if (sig) {
+			// Windows has no signals. `child.kill()` there is TerminateProcess,
+			// so Ctrl+C killed the shell outright instead of interrupting what
+			// it was running, and the byte never reached the child at all. Let
+			// it through and leave it to the shell, which is what a console
+			// would do.
+			if (sig && process.platform !== "win32") {
 				this._flushLineBuffer();
 				try {
 					this._child.kill(sig);
